function redirect() {
  //In this function we will cover how to use a button to redirect a 
}

function arrow(button) {
  //in this function we will be displaying what is inside an HTML element
};

//This is called an onload function where when the window is loaded this function runs
window.onload = function() {
  //in this function we will show you how to select certain elements in and HTML page using Javascript
};
